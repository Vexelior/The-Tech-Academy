namespace Class
{
    internal static class StaticClass
    {
        //Declaring the class 'StaticClass' as static.
        //A static class can only have static data members, static methods, and a static constructor.
        //You cannot inherit from another class.
    }
}